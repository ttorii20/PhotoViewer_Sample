# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PhotoViewer_sample' do
    # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
    use_frameworks!
    
    pod 'SDWebImage'
    # Pods for PhotoViewer_sample
    
    target 'PhotoViewer_sampleTests' do
        inherit! :search_paths
        # Pods for testing
    end
    
    target 'PhotoViewer_sampleUITests' do
        inherit! :search_paths
        # Pods for testing
    end
    
end
